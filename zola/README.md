# muster.chat Zola site

## Requirements

If using `devbox` then:
- Nix installed
- Devbox installed

Otherwise:
- Zola installed

## Getting started

If using `devbox` run:

```sh
devbox shell
```

Then to start the site locally:

```sh
zola serve
```
